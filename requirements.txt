# WAJIB headless untuk Streamlit Cloud / server tanpa GUI (libGL.so.1 tidak ada)
opencv-contrib-python-headless>=4.9
streamlit>=1.48
numpy
spandrel>=0.4
imageio-ffmpeg

# PyTorch CPU — pakai index resmi supaya tidak menarik paket CUDA (~2,5 GB)
# yang akan melebihi kuota disk Streamlit Cloud.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# (Opsional, khusus mode eksperimental SUPIR — butuh GPU, tidak di Streamlit Cloud)
# xformers
